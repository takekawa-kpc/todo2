# シンプルな ToDo アプリ 仕様書

## 1. 概要

シンプルな ToDo を記録・管理できる Web アプリケーションです。
データはブラウザの localStorage に保存し、サーバー不要で PC 単体で完結します。

## 2. 前提条件

- 利用環境: PC（デスクトップブラウザ）のみを想定
  - 対象: Chrome / Edge / Firefox の最新 2 世代
  - モバイル・タブレットのレスポンシブ対応は必須ではない（ただし画面が狭くても壊れないこと）
- サーバー・バックエンドなし（完全クライアントサイド）
- 認証機能なし（ローカル 1 ユーザー分のみ）

## 3. 技術スタック

- React
- Vite
- Tailwind CSS
- デザイン方針: モダンでミニマル、ダークモード対応、美しいアニメーション
- テスト: Playwright（テストコードは TypeScript）

## 4. 機能要件

### 4.1 ToDo の追加
- 入力欄にテキストを入力し、Enter キーまたは「追加」ボタンで追加できる
- 空文字・空白のみの入力は無視される
- 入力には最大 200 文字を制限する
- 追加後は入力欄が空になり、フォーカスが維持される

### 4.2 ToDo の表示
- 追加順（新しい順 or 古い順のいずれか、固定）に一覧表示する
- 各項目には以下を表示する:
  - 本文
  - 作成日時（例: `2026/09/22 14:30`）
  - 完了状態（チェックボックス）
- 一覧が空の場合は「ToDo ありません」のような空状態を表示する

### 4.3 完了 / 未完了の切り替え
- チェックボックスで切り替えられる
- 完了した項目は視覚的に区別できる（例: 取り消し線・色薄め）
- 切り替えは即座に localStorage に反映される

### 4.4 ToDo の削除
- 各項目の削除ボタンで削除できる
- 削除は即座に反映される（確認ダイアログは不要、ただし必要に応じて undo を検討）

### 4.5 ToDo の編集
- 項目の本文を後から編集できる（インライン編集 or 編集モーダル）
- Enter または保存ボタンで確定、Escape でキャンセル

### 4.6 フィルタ表示（推奨）
- 「すべて / 未完了 / 完了」の 3 つで切り替えられる

### 4.7 一括操作（任意）
- 完了済み項目のすべてを削除するボタン

## 5. データ仕様

### 5.1 保存先
- ブラウザの localStorage
- キー: `todo-app.items`（キー名は調整可）

### 5.2 データ構造
```json
{
  "items": [
    {
      "id": "uuid などの一意 ID",
      "text": "買い物リストを作成", 
      "done": false,
      "createdAt": "2026-09-22T14:30:00.000Z",
      "updatedAt": "2026-09-22T14:30:00.000Z"
    }
  ]
}
```

- `id`: 一意識別子（`crypto.randomUUID()` を推奨）
- `text`: ToDo の本文（最大 200 文字）
- `done`: 完了フラグ
- `createdAt` / `updatedAt`: ISO 8601 形式のタイムスタンプ

### 5.3 保存タイミング
- 追加・編集・削除・完了切り替えた直後に保存
- ページ読み込み時に localStorage から読み込み、パース失敗時は空リストで初期化し、壊れたデータをバックアップ（例: `todo-app.items.bak`）する

### 5.4 容量対策
- 項目数・本文長の上限を守ることで容量超過を回避する
- `QuotaExceededError` 発生時はエラーメッセージを表示し、既存データは保持する

## 6. UI / デザイン

- モダンでミニマルなデザイン
- ダークモード対応（OS/ブラウザ設定に追随、または手動切替）
- 追加・削除・完了切り替え時の軽いアニメーション（fade、slide など）
- 中央寄せの 1 カラムレイアウト（最大幅 640px 程度）
- 主要な操作はキーボードのみでも完結できる（Tab 移動、Enter で追加、Escape で編集取消）
- 可読性のため十分なコントラスト比を保証

### 画面構成
- ヘッダー: タイトル「ToDo」
- 入力エリア: テキスト入力 + 追加ボタン
- フィルタ: すべて / 未完了 / 完了
- 一覧: 各項目（チェックボックス、本文、作成日時、編集・削除ボタン）
- フッター: 残件数表示、完了済みの一括削除ボタン（任意）

## 7. 非機能要件

- パフォーマンス: 100 項目程度まで円滑に動作する
- アクセシビリティ: 意味ある見出し、ラベル、aria 属性、フォーカスリング
- セキュリティ: ユーザー入力の XSS 防止（React のエスケープに委ねる、`dangerouslySetInnerHTML` 不使用）
- 耐障害性: localStorage のデータ破損時にアプリがクラッシュしない

## 8. 受け入れ基準

- [ ] ToDo を追加・編集・削除・完了切り替えできる
- [ ] ページを再読み込みするとデータが保持される
- [ ] 空状態、エラー状態が適切に表示される
- [ ] ダークモードで利用可能
- [ ] キーボードのみで主要操作が完結する
- [ ] Playwright テストで主要フロー（追加/完了/削除）を検証できる

## 9. 実装範囲外（v1 ではやらないこと）

- アカウント・同期・クラウド保存
- 複数リスト・カテゴリ・ラベル
- 期限リマインド・通知
- 他ブラウザ間・他デバイス間の同期
- ドラッグ & ドロップによる並び替え

## 10. 今後の拡張候補

- 期限日時・優先度
- 並び替え（ドラッグ & ドロップ）
- インポート / エクスポート（JSON / CSV）
- コマンドパレット（`/` でフォーカス等）
